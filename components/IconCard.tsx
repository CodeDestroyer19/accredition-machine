import React from "react";
import { Card } from "@material-ui/core";

const IconCard = ({ image }) => {
  return <Card style={{ padding: "0.5rem", margin: "0px 15px" }}>{image}</Card>;
};

export default IconCard;
